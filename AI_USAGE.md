# AI 使用说明

## 使用的 AI

本任务使用了 OpenAI Codex 作为辅助工具。

## AI 协助内容

- 阅读并拆分题目需求，整理业务规则、边界条件和交付清单；
- 起草 `PLAN.md`；
- 辅助设计状态归属、事件处理流程和模块边界；
- 生成 Python 实现和 `unittest` 测试初稿；
- 根据逐步检查结果补充多人、重复事件、超时边界以及对话/会议交叉抑制测试；
- 起草架构说明和 ROS 2 日志诊断答案；

AI 没有连接或控制真实机器人，也没有把 `accepted_async` 当作动作完成证据。

## 验证方式

- 使用 Python 3.11.15 的项目虚拟环境导入并运行代码；
- 使用标准库 `unittest` 执行全部 13 个自动化测试；
- 单独验证不同 `RobotApplication` 实例不共享状态；
- 修改 `snapshot()` 返回值后重新获取快照，确认内部状态不变；
- 使用题目给定的完整事件时间线核对每一步输出；
- 使用 `git diff` 和 `git diff --check` 检查实际文件变更及空白错误；
- 根据 Action Client/Server 数量、systemd 服务状态和异步语义复核 ROS 2 结论，并把事实、推断和待验证项分开记录。
