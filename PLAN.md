# 实施计划

## 目标

在 Python 3.10+ 环境中实现一个与 ROS 2、相机及硬件 SDK 解耦的机器人迎宾应用。应用接收人员、对话、会议和计时事件，维护单次接待过程的状态，并只返回当前事件新产生的效果。

## 范围

本次实现包括：

- `Event`、`Effect` 公共数据模型；
- `RobotApplication` 及其事件处理和状态快照接口；
- 首次迎宾、重复进入去重、对话/会议抑制、延迟送客、短暂离场返回和新一轮接待规则；
- 自动化测试及题目指定的完整事件序列；
- 架构说明、完成报告、ROS 2 日志判断和 AI 使用说明。


## 实施步骤

1. 建立项目结构和公共模型
   - 创建 `robot_application/` 与 `tests/`；
   - 按指定导入路径实现不可变的 `Event` 和 `Effect`；
   - 保留 `RobotApplication(absence_timeout_s=10.0)` 公共接口。

2. 设计并实现状态管理
   - 由每个 `RobotApplication` 实例独立持有人员在场、接待周期、对话、会议、离场计时和送客去重状态；
   - 不使用全局可变状态；
   - 将输出抑制判断与接待周期状态转换保持清晰分离。

3. 实现事件处理规则
   - 首次进入时产生挥手和“欢迎光临”；
   - 同一接待周期内重复进入不重复迎宾；
   - 对话或会议期间不产生迎宾、送客或机器人动作，结束后不补发；
   - 人员离开满配置的超时时间后，由 `TICK` 触发送客且只触发一次；
   - 短暂离开后返回时取消待确认离场，不送客且不重复迎宾；
   - 完成离场确认后，再次进入开启新的接待周期。

4. 实现安全状态快照
   - `snapshot()` 返回不可变状态对象或与内部状态隔离的副本；
   - 验证调用方不能通过修改快照影响应用内部状态。

5. 编写自动化测试
   - 覆盖首次迎宾和重复进入；
   - 覆盖对话与会议抑制及不补发行为；
   - 覆盖离场不足、恰好达到和超过 10 秒的边界；
   - 覆盖送客去重、短暂离场返回和完整离场后重新迎宾；
   - 覆盖题目给出的完整事件序列；
   - 覆盖 `snapshot()` 隔离和自定义超时时间。

6. 完成文档和验证
   - 在 `ARCHITECTURE.md` 说明模块边界、依赖方向、状态归属以及 VIP、RAG、ROS 2 导航的扩展点；
   - 在 `REPORT.md` 记录完成情况、测试命令和结果、未完成项，并按事实、推断和待验证项回答 ROS 2 判断题；
   - 在 `AI_USAGE.md` 如实记录 AI 的用途、人工复核和验证方式；
   - 执行完整测试并检查最终交付文件。

## 暂定设计与假设

- 以 `RobotApplication` 作为业务用例入口，核心代码只产生 `Effect`，不直接执行真实机器人动作；
- 对话和会议分别维护状态，只要任一活动仍在进行就抑制输出；
- 离场超时采用 `TICK.timestamp - PERSON_LEFT.timestamp >= absence_timeout_s` 判断，因此恰好达到 10 秒时应送客；
- `person_id` 保留在事件模型中。题目未明确多人同时在场时的聚合语义，首版按单个接待区域/单一在场会话处理，并在架构和报告中明确该假设；
- 对题目未指定的效果类型、送客内容、未知事件及异常事件顺序，将选择一致且可测试的行为，并在代码或报告中说明。

## 验证标准

- 所有题目要求的文件和公共导入路径存在；
- `handle_event()` 每次只返回本次事件产生的新效果；
- 题目给出的时间序列输出完全匹配；
- 规定的业务场景和边界测试全部通过；
- 修改 `snapshot()` 返回值不会改变内部状态；
- 业务模块不存在 ROS 2、相机或硬件 SDK 依赖；
- `REPORT.md` 中的测试结果与实际执行结果一致。

## 时间安排

- 0–15 分钟：确认需求、提交本计划并创建 Draft PR；
- 15–65 分钟：实现模型、状态管理和事件处理；
- 65–90 分钟：完成测试并修正边界问题；
- 90–110 分钟：完成架构、ROS 2 分析和 AI 使用说明；
- 110–120 分钟：执行最终测试、核对交付清单并将 PR 标记为 Ready for review。
